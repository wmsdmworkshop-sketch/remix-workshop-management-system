# DWIP Enterprise — Development Profile
# -------------------------------------------------------------
NODE_ENV=development
PORT=3001

# Dynamic in-memory testing parameters
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=localdevsecret
DB_DATABASE=dwip_dev

# Auth secrets (Development stubs)
JWT_SECRET=dev_only_jwt_secret_signing_key_dwip_enterprise_100
CUSTOMER_JWT_SECRET=dev_only_customer_jwt_secret_signing_key_dwip_enterprise_200

# Optional services configurations
GEMINI_API_KEY=
ADDITIONAL_CORS_ORIGINS=http://localhost:3000,http://localhost:5173
VITE_WORKFORCE_PROFILE=dev

